export interface MousePosition {
  x: number;
  y: number;
}

export interface NavigationItem {
  label: string;
  href: string;
}

export enum Section {
  HOME = 'HOME',
  WORK = 'WORK',
  CONTACT = 'CONTACT'
}